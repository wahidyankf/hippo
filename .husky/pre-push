set -e

# The last local moment before anything reaches the remote, which for a public
# repository is the moment publication begins.
#
# Rhino alone consumes Git's update records and binds the declared immutable
# range; remote names and URLs never reach child gate argv.
exec ./rhino gate run --surface pre-push --push-updates-stdin
