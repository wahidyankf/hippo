set -e

# Rhino proves Git supplied this worktree's canonical COMMIT_EDITMSG path before
# binding its typed message to the declared gates.
exec ./rhino gate run --surface commit-msg --message-file "$1"
