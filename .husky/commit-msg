set -e

# Git's argument is the path to the message being written, forwarded unchanged
# so a gate that reads the message file gets the path Git handed the hook.
exec ./rhino gate run --surface commit-msg -- "$@"
